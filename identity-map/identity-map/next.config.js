/**
 * @type {import('next').NextConfig}
 *
 * This configuration enables React Strict Mode and leaves the default
 * behaviour unchanged.  You can add custom configuration here as
 * additional increments introduce new requirements such as rewrites or
 * image domains.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
